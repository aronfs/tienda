meta {
  name: Clients
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/analytics/clients
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:query {
  period: this_month
}