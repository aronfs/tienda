meta {
  name: Inventory
  type: http
  seq: 10
}

get {
  url: {{base_url}}/api/analytics/inventory
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:query {
  period: this_month
}