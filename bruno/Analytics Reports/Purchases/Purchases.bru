meta {
  name: Purchases
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/analytics/purchases
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:query {
  period: this_month
}