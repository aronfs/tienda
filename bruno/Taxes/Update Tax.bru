meta {
  name: Update Tax
  type: http
  seq: 16
}

put {
  url: {{base_url}}/api/taxes/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "rate": 12,
    "name": "IVA General (Actualizado)"
  }
}