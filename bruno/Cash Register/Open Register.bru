meta {
  name: Open Register
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/cash-register/open
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "initialAmount": 500.00
  }
}
