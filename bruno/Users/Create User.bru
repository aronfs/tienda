meta {
  name: Create User
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Usuario Nuevo",
    "email": "usuario@email.com",
    "password": "Password123!",
    "roleId": 2
  }
}
